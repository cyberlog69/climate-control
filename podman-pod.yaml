# Native Podman / Kubernetes Pod Manifest
# Run natively using: podman play kube podman-pod.yaml
# Stop and tear down using: podman play kube --down podman-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: climate-control-pod
  labels:
    app.kubernetes.io/name: climate-control
spec:
  containers:
    - name: climate-control
      image: localhost/climate-control:latest
      ports:
        - name: http
          containerPort: 8080
          hostPort: 8080
      resources:
        limits:
          memory: 256Mi
        requests:
          memory: 64Mi
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: false
